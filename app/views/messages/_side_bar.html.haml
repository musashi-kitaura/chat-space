.Side-ber
    .LeftHeader 
        .LeftHeader-list
            .LeftHeader-list__username
                = current_user.name
            .LeftHeader-list__iconlist
                .LeftHeader__icon
                    = link_to "#" do
                        = icon('fas', 'edit' )

                .LeftHeader__icon   
                    = link_to edit_user_path(current_user) do
                        =icon('fas','cog' )

    .Group-list
        - current_user.groups.each do |group|
            .Groups
                = link_to '#', class:Group do    
                    .Group__name
                        = group.name
                    .Group__messages
                        まだメッセージはありません。
